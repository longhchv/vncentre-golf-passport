# Chép file này thành .env.local rồi điền giá trị từ Supabase → Project Settings → API.
# KHÔNG đưa service_role key vào đây (key này chỉ dùng phía máy chủ).
VITE_SUPABASE_URL=https://xxxxxxxx.supabase.co
VITE_SUPABASE_ANON_KEY=
# local | staging | production
VITE_APP_ENV=local
